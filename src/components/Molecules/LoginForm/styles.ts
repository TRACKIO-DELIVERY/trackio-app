import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  form: {
    paddingTop: 98,
    gap: 16,
  },
  inputsContainer: {
    paddingBottom: 50,
  },
  linkToRegister: {
    alignSelf: "center",
  },
});
