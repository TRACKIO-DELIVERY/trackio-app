import { StyleSheet } from "react-native-unistyles";

export const styles = StyleSheet.create({
  form: {
    gap: 16,
  },
  inputsContainer: {
    paddingBottom: 50,
  },
  linkToRegister: {
    alignSelf: "center",
  },
  inputView: {
    gap: 5,
    marginBottom: 2,
  },
});
